---
import LinkInline from "./LinkInline.astro"

interface Props {
  title: string
  company: string
  description: string
  link?: string
  date: string
  technologies?: string[]
  achievements?: string[]
}

const { title, company, description, link } = Astro.props
---

<h3 class="text-xl font-bold bg-clip-text text-transparent bg-gradient-to-r from-indigo-600 to-purple-700">{title}</h3>
<h4 class="font-semibold text-lg text-gray-700 dark:text-gray-300 mb-2">{company}</h4>
<p class="text-base font-normal text-gray-600 dark:text-gray-300">
  {description}
</p>
{
  link && (
    <div class="mt-4">
      <LinkInline href={link} class="text-indigo-600 hover:text-purple-600 transition-colors">
        Saber más{" "}
        <svg
          xmlns="http://www.w3.org/2000/svg"
          class="w-5 icon icon-tabler icon-tabler-chevron-right"
          width="24"
          height="24"
          viewBox="0 0 24 24"
          stroke-width="2"
          stroke="currentColor"
          fill="none"
          stroke-linecap="round"
          stroke-linejoin="round"
        >
          <>
            <path stroke="none" d="M0 0h24v24H0z" fill="none" />
            <path d="M9 6l6 6l-6 6" />
          </>
        </svg>
      </LinkInline>
    </div>
  )
}
